/** Pixel distance from left edge within which the mouse opens the panel. */
export const PANEL_EDGE_THRESHOLD_PX = 24;

/** Delay in ms after mouse leaves panel/edge before the panel closes. */
export const PANEL_CLOSE_DELAY_MS = 500;
